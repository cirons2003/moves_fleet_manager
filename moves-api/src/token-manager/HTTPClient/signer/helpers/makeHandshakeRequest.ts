import { Domain, RoutableMessage } from '../protobuf/outputs/universal_message';

export const makeHandshakeRequest = (
    domain: Domain,
    routingAddress: Uint8Array,
    publicKey: string,
) => {};
